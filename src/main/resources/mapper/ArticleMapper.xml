<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">

    <!-- 结果映射 -->
    <resultMap id="ArticleVOResultMap" type="com.blog.vo.ArticleVO">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="summary" column="summary"/>
        <result property="coverImage" column="cover_image"/>
        <result property="viewCount" column="view_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="commentCount" column="comment_count"/>
        <result property="isTop" column="is_top"/>
        <result property="createTime" column="create_time"/>
        <result property="categoryName" column="category_name"/>
        <result property="author" column="author"/>
    </resultMap>

    <!-- 分页查询文章列表（多条件） -->
    <select id="pageList" resultMap="ArticleVOResultMap">
        SELECT 
            a.id,
            a.title,
            a.summary,
            a.cover_image,
            a.view_count,
            a.like_count,
            a.comment_count,
            a.is_top,
            a.create_time,
            c.name AS category_name,
            a.author
        FROM blog_article a
        LEFT JOIN blog_category c ON a.category_id = c.id
        <where>
            a.deleted = 0
            <if test="query.isPublished != null">
                AND a.is_published = #{query.isPublished}
            </if>
            <if test="query.categoryId != null">
                AND a.category_id = #{query.categoryId}
                <!-- 优化：使用复合索引 idx_category_published -->
            </if>
            <if test="query.author != null and query.author != ''">
                AND a.author LIKE CONCAT('%', #{query.author}, '%')
            </if>
            <if test="query.title != null and query.title != ''">
                AND a.title LIKE CONCAT('%', #{query.title}, '%')
                <!-- 优化：title 字段有索引 idx_title -->
            </if>
            <if test="query.isTop != null">
                AND a.is_top = #{query.isTop}
            </if>
            <if test="query.startTime != null">
                AND a.create_time &gt;= #{query.startTime}
            </if>
            <if test="query.endTime != null">
                AND a.create_time &lt;= #{query.endTime}
            </if>
        </where>
        ORDER BY 
            a.is_top DESC,
            a.create_time DESC
    </select>

    <!-- 批量更新浏览量 -->
    <update id="batchUpdateViewCounts">
        UPDATE blog_article SET view_count = CASE id
        <foreach collection="viewCounts" item="count" index="articleId">
            WHEN #{articleId} THEN #{count}
        </foreach>
        END
        WHERE id IN
        <foreach collection="viewCounts" item="count" index="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </update>

</mapper>
